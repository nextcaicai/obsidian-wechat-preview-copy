{
	"name": "obsidian-wechat-preview",
	"version": "0.1.5",
	"description": "Preview the active Obsidian note in the right sidebar and copy rich HTML for WeChat Official Account.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"wechat",
		"markdown"
	],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.15.30",
		"builtin-modules": "^5.0.0",
		"esbuild": "^0.25.5",
		"obsidian": "^1.8.7",
		"typescript": "^5.8.3"
	}
}
